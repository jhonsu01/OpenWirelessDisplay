<Window x:Class="OpenWirelessDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenWirelessDisplay - Servidor" Height="560" Width="780"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🖥️" FontSize="26" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <StackPanel>
                <TextBlock Text="OpenWirelessDisplay" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Monitor secundario inalambrico - Servidor Windows"
                           Foreground="{StaticResource MutedBrush}" FontSize="12"/>
            </StackPanel>
        </StackPanel>

        <!-- Panel principal -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1.4*"/>
            </Grid.ColumnDefinitions>

            <!-- Tarjeta PIN -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="PIN DE EMPAREJAMIENTO" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource MutedBrush}"/>
                    <TextBlock x:Name="PinText" Text="------" FontSize="46" FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}" Margin="0,4,0,4"
                               FontFamily="Consolas"/>
                    <TextBlock Text="Introduce este PIN en la app Android para emparejar."
                               Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Tarjeta estado -->
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="ESTADO DEL SERVIDOR" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource MutedBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Ellipse x:Name="StatusDot" Width="12" Height="12" Fill="{StaticResource DangerBrush}"
                                 VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="StatusText" Text="Detenido" FontSize="16" FontWeight="SemiBold"/>
                    </StackPanel>
                    <TextBlock x:Name="InfoText" Text="IP: -    Puerto: -    Clientes: 0"
                               Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,8,0,12"/>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="StartButton" Content="Iniciar" Style="{StaticResource AccentButton}"
                                Width="120" Click="StartButton_Click"/>
                        <Button x:Name="StopButton" Content="Detener" Style="{StaticResource AccentButton}"
                                Width="120" Margin="10,0,0,0" Click="StopButton_Click" IsEnabled="False"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Log -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DockPanel>
                <TextBlock Text="REGISTRO" FontSize="11" FontWeight="SemiBold" DockPanel.Dock="Top"
                           Foreground="{StaticResource MutedBrush}" Margin="0,0,0,8"/>
                <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="LogBox" IsReadOnly="True" Background="Transparent"
                             Foreground="{StaticResource TextBrush}" BorderThickness="0"
                             FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Window>
